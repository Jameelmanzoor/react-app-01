// we are creating a new React component

import React from 'react';

function SayHello({firstName}) {
    return <p>This is para in sayHello function. firstName = {firstName}</p>
}

export default SayHello;